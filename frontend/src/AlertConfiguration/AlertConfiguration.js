import React from 'react';
import ReactDOM from 'react-dom';
import AlertConfigurationApp from './AlertConfigurationComponents/AlertConfigurationApp';

ReactDOM.render(
  <React.StrictMode>
    <AlertConfigurationApp/>
  </React.StrictMode>,
  document.getElementById('root')
);
